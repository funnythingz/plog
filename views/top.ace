= content main
  h1
    | log.link
  section
    a href="/new"
      | create Entry
  {{if .}}
    section
      {{range .}}
        article
          a href="/{{.Id}}"
            {{.Title}}
      {{end}}
  {{end}}
