= content main
  h1
    | log.link
  section
    a href="/new"
      | create Entry
  {{if not .NotFound}}
    section
      {{range .Entries}}
        article
          a href="/{{.Id}}"
            {{.Title}}
      {{end}}
  {{end}}
