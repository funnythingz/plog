= content main
  header.header
    h1.logo
      | plog
    p.lead.description
      | is a simple diary for people all over the world.
  section
    section.action
      .row
        .col-sm-6.col-sm-offset-3
          a.btn.btn-lg.btn-block.btn-primary.create href="/new"
            | create
    section
      .row
        {{range .Entries}}
          article.panel.panel-default.entry-item
            a href="/{{.Id}}"
              .panel-heading
                {{.Title}}
        {{end}}
    nav
      ul.pager
        {{if not .Paginate.IsFirstpoint}}
          li.previous
            a href="?page={{.Paginate.PrevPage}}"
              i.fa.fa-arrow-left
              span.prev-text
                | prev
        {{end}}
        {{if not .Paginate.IsEndpoint}}
          li.next
            a href="?page={{.Paginate.NextPage}}"
              span.next-text
                | next
              i.fa.fa-arrow-right
        {{end}}
